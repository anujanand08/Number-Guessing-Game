# Number-Guessing-Game
This is a classic number guessing game where the computer generates a random number within a user-defined range (e.g., 1 to 100). Your goal is to guess the number in as few attempts as possible using intelligent feedback after each try.
